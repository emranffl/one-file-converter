# + This script is used to disable commiting from the VS Code source control.

#!/usr/bin/env sh
. "$(dirname -- "$0")/lib/constants"

#!/bin/sh

# exec < /dev/tty || true
# exec < /dev/tty && npx cz --hook || true
